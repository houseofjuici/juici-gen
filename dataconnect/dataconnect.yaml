specVersion: "v1"
serviceId: "juici"
location: "us-east1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "juici"
      cloudSql:
        instanceId: "juici"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
